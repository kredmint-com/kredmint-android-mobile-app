import React from 'react';
import { Image, View } from 'react-native';
import Onboarding from 'react-native-onboarding-swiper';

const Payment = ({ onDoneCb }) => {
  return (
    <View>
      <Text>This is payment page </Text>
    </View>
  );
};

export default Payment;
